{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run --sort build",
    "test": "lerna run --sort test"
  },
  "devDependencies": {
    "lerna": "3.16.4"
  }
}
