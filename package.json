{
    "name": "itmat-broker",
    "private": true,
    "scripts": {
        "postinstall": "npm run bootstrap",
        "bootstrap": "lerna bootstrap --hoist",
        "clean": "lerna run clean",
        "start": "lerna run --stream --parallel --scope itmat-commons --scope itmat-utils --scope itmat-interface --scope itmat-ui-react start",
        "build": "lerna run --stream --ignore itmat-query-language --ignore itmat-job-executor --ignore ukb-curator build",
        "publish": "lerna run tsc && lerna publish",
        "test": "echo There is no test at this point !"
    },
    "optionalDependencies": {
        "mongodb-client-encryption": "0.3.1"
    },
    "devDependencies": {
        "lerna": "3.18.4",
        "tslint": "5.20.1"
    }
}