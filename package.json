{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .jsx --ext .js ./packages/",
    "lint:fix": "eslint --ext .jsx --ext .js --fix ./packages/",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  }
}
