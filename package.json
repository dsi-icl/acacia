{
    "name": "itmat-broker",
    "private": true,
    "scripts": {
        "postinstall": "npm run bootstrap",
        "bootstrap": "lerna bootstrap",
        "clean": "lerna run clean",
        "start": "lerna run --stream --parallel --scope itmat-commons --scope itmat-utils --scope itmat-job-executor --scope itmat-interface --scope itmat-ui-react start",
        "build": "lerna run --stream build",
        "publish": "lerna run tsc && lerna publish",
        "test:startCypress": "lerna run --scope itmat-ui-react test:cypress",
        "test:unit": "lerna run --stream --parallel --scope itmat-interface --scope itmat-job-executor --scope itmat-utils --scope itmat-ui-react test",
        "test:e2e": "start-server-and-test start http://localhost:3000 test:startCypress",
        "test": "npm run test:unit && npm run test:e2e"
    },
    "optionalDependencies": {
        "mongodb-client-encryption": "1.0.1"
    },
    "devDependencies": {
        "@types/jest": "25.2.1",
        "eslint": "6.8.0",
        "eslint-plugin-jest": "23.8.2",
        "jest-environment-node": "25.3.0",
        "lerna": "3.20.2",
        "tslint": "6.1.1",
        "start-server-and-test": "1.10.11"
    }
}
